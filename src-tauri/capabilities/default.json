{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: app commands, events, and the folder picker only. No fs/shell/http from the frontend.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:default",
    "dialog:allow-open"
  ]
}
